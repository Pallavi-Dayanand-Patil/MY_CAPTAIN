# Displaying fibonacci series as per the user input 

# main function
def fibonacci(n):
    if(n <= 1):
        return n
    else:
        return(fibonacci(n-1) + fibonacci(n-2))
n = int(input("Enter number of terms to be displayed:"))  # user input
print("Fibonacci sequence:")
for i in range(n):
    print(fibonacci(i))
